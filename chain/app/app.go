// Package app contains the LumenChain application wiring.
package app

// App will host the Cosmos SDK application configuration.
// The full module wiring is intentionally introduced incrementally.
type App struct{}
