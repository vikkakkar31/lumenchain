# LumenChain chain bootstrap

This file documents the generated-chain boundary for Devnet v0.1.

The bootstrap workflow generates the runnable Cosmos SDK application from the pinned Ignite template before implementation work is layered on top.
